<div class="top_nav">
  <div class="nav_menu">
    <div class="nav toggle">
      <div class="dropdown menu_toggle">
        <span class="dropdown-toggle" type="button" data-toggle="dropdown" aria-expanded="false">
          <%= image_tag "language.png" %>
          <%= t ".language" %>
        </span>
        <div class="dropdown-menu dropdown-usermenu pull-right" aria-labelledby="navbarDropdown">
            <%= link_to t(".english"), url_for(locale: :en), class:"dropdown-item" %>
            <%= link_to t(".vietnamese"), url_for(locale: :vi), class:"dropdown-item" %>
        </div>
      </div>
    </div>
    <nav class="nav navbar-nav">
      <ul class=" navbar-right">
        <li class="nav-item dropdown open" style="padding-left: 15px;">
          <%= link_to url_for("#"),class:"user-profile dropdown-toggle", data:{toggle:"dropdown"} do %>
            <%= image_tag "admin/img.jpg" %><%= current_user.fullname %>
          <% end %>
          <div class="dropdown-menu dropdown-usermenu pull-right" aria-labelledby="navbarDropdown">
            <%= link_to t(".profile"), url_for("#"), class:"dropdown-item" %>
            <%= link_to t(".log_out"), logout_path, method: :delete, class:"dropdown-item" %>
          </div>
        </li>
      </ul>
    </nav>
  </div>
</div>
